{
  "name": "ytrc-server",
  "type": "module",
  "version": "v0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}